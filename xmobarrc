Config {
    font         = "xft:Terminus:pixelsize=16,-*-*-*-r-*-*-16-*-*-*-*-*-*-*",
    bgColor      = "#1c1c1c",
    fgColor      = "#ffffff",
    position     = TopSize C 100 26,
    lowerOnStart = True,
    commands     = [
        Run StdinReader,
        Run Date "%a %b %_d %Y <fc=#7071B8>%H:%M:%S</fc>" "date" 10
        ],
    sepChar      = "%",
    alignSep     = "}{",
    template     = "%StdinReader% }{ %date% || %acpi%"
}

