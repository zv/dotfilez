These were generated with the tool `dconf dump`


The files here can have their underscore ("\_") replaced with a slash "/" and restored

- '''org_gnome_terminal_legacy_profiles:'''
  The gnome terminal preferences
